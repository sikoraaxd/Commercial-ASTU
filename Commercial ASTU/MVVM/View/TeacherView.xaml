<UserControl x:Class="Commercial_ASTU.MVVM.View.TeacherView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Commercial_ASTU.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="1096" d:DesignWidth="640">
    <Grid>
        <ListView>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" Width="1" DisplayMemberBinding="{Binding ID}"/>
                    <GridViewColumn Header="Номер заявки" Width="213" DisplayMemberBinding="{Binding FIO}"/>
                    <GridViewColumn Header="Информация" Width="213" DisplayMemberBinding="{Binding Login}"/>
                    <GridViewColumn Header="Действие" Width="213" DisplayMemberBinding="{Binding Password}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
